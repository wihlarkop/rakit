name: Task7 Type Fix (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Task 7 typing helper is retired and kept inert until branch cleanup."
