name: D3.3 Peewee format

on:
  push:
    branches: [phase-d3-3-peewee]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: phase-d3-3-peewee
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.12"
      - run: uv sync --all-packages --dev --locked
      - run: uv run ruff format packages/rakit-peewee packages/rakit/src/rakit/_install.py
      - name: Commit canonical formatting
        run: |
          set -euo pipefail
          if git diff --quiet -- packages/rakit-peewee packages/rakit/src/rakit/_install.py; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/rakit-peewee packages/rakit/src/rakit/_install.py
          git commit -m "style(peewee): format permanent regression suite"
          git push origin HEAD:phase-d3-3-peewee
