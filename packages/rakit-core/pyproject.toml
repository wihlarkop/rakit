[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "rakit-core"
version = "0.1.0a1"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11,<3", "cryptography>=43"]

[tool.hatch.build.targets.wheel]
packages = ["src/rakit_core"]
