# C3 Source Milestone

Before regression tests are changed, the branch source includes:

- canonical six-extra package metadata;
- server-neutral `standard` bundle;
- internal typed install vocabulary;
- capability-specific optional dependency diagnostics;
- canonical C2 scaffold dependency selection;
- uv-first installation documentation.

Next gate: temporary source-smoke execution. Regression tests remain intentionally untouched until that gate passes.
